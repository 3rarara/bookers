<h1>Books</h1>

<h4>Title</h4>

<h4>Body</h4>

<% @books.each do |book| %>

  <span><%= book.title %></span>

  <span><%= book.body %></span>

  <span>
    <%= link_to "Show", "/books/#{book.id}" %>
    <%= link_to "Edit", "/books/#{book.id}/edit" %>
  </span>

<% end %>